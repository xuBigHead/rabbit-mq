server:
  port: 8023
spring:
  application:
    name: rabbit-consumer-three
  profiles:
    active: dev
  rabbitmq:
    host: localhost
    port: 5672
    username: root
    password: root
#    virtual-host: my_host
#    requested-heartbeat: 20
#    publisher-returns: true
#    connection-timeout: 0
logging:
  config: classpath:logback-spring-dev.xml